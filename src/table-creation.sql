USE crm;

-- CREATE TABLE owner(
-- id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
-- name VARCHAR(20)    
-- );

-- CREATE TABLE client(
-- id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
-- name varchar (20),
-- email varchar (50),
-- firstContact Date,
-- emailType varchar(4),
-- sold Boolean,
-- country varchar(20),
-- owner INT,

-- FOREIGN KEY (owner) REFERENCES owner(id)
-- );

-- ALTER TABLE client
-- -- DROP COLUMN emailType;
-- ADD COLUMN emailType varchar(4)

SELECT *  FROM client;
